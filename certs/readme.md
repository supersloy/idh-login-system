# Put your PEM certificates here
They are not pushed to the remote due to `.gitignore`
Name of sertificate files: `server.crt.pem` and `server.key.pem`
Update permissions of `server.key.pem` to `755`.